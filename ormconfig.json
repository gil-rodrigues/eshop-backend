{
  "name": "default",
  "type": "postgres",
  "host": "localhost",
  "port": "5432",
  "username": "postgres",
  "password": "docker",
  "database": "postgres",
  "logging": [
    "query"
  ],
  "entities": [
    "./src/datasource/typeorm/entities/*.ts"
  ],
  "migrations": [
    "./src/datasource/typeorm/migration/*.ts"
  ],
  "cli": {
    "migrationsDir": "./src/datasource/typeorm/migration/"
  }
}
